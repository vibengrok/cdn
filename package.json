{
  "name": "json-api-server",
  "version": "1.0.0",
  "description": "A simple JSON API server hosted on Netlify",
  "main": "index.js",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step required'",
    "deploy": "netlify deploy --prod"
  },
  "keywords": [
    "json",
    "api",
    "netlify",
    "serverless"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
